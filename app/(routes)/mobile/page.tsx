import { CORE_MOBILE_METADATA, CoreMobileScreen } from '../../src/modules/core-mobile';

export const metadata = CORE_MOBILE_METADATA;

export default function Page() {
  return <CoreMobileScreen />;
}

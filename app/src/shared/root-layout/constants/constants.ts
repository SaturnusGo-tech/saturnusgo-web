import type { Metadata } from 'next';

export const ROOT_LAYOUT_LANGUAGE = 'en';

export const ROOT_METADATA: Metadata = {
  title: 'SaturnusGo',
  description: 'Clean empty route shell for SaturnusGo.',
};
